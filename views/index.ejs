<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cocktail Recipe Generator</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1 class="text-center my-4">Random Cocktail Recipe</h1>
    <div class="text-center mb-4">
      <a href="/get-cocktail" class="btn btn-primary">Get a Random Cocktail</a>
    </div>
    <% if (cocktail) { %>
      <div class="card mx-auto" style="max-width: 500px;">
        <img src="<%= cocktail.strDrinkThumb %>" class="card-img-top" alt="<%= cocktail.strDrink %>">
        <div class="card-body">
          <h2 class="card-title"><%= cocktail.strDrink %></h2>
          <h5>Ingredients:</h5>
          <ul>
            <% for (let i = 1; i <= 15; i++) { %>
              <% if (cocktail[`strIngredient${i}`]) { %>
                <li><%= cocktail[`strIngredient${i}`] %> <%= cocktail[`strMeasure${i}`] || '' %></li>
              <% } %>
            <% } %>
          </ul>
          <h5>Instructions:</h5>
          <p><%= cocktail.strInstructions %></p>
        </div>
      </div>
    <% } else if (error) { %>
      <div class="alert alert-danger text-center"><%= error %></div>
    <% } else { %>
      <p class="text-center">Click the button to get a random cocktail recipe!</p>
    <% } %>
  </div>
</body>
</html>
